<div class="auther">
    <h1 class="auther__heading">Welcome To ABlog</h1>
    <p class="auther__body">enjog this bloy</p>
</div>

<img src="/img/moo.jpeg" alt="the most delicious food is MooKrata" class="moo" width="981" height="582">

<section class="articles">
    <h2 class="articles__heading">Latest Posts</h2>
  
    <ul class="article-ul">
      <% data.forEach(post => { %>
        <li>
          <a href="/post/<%= post._id %>">
            <span><%= post.title %></span>
            <span class="article-list__date"><%= post.createdAt.toDateString() %></span>
          </a>
        </li>
      <% }) %>
    </ul>
  
  

    <% if (nextPage !== null) { %>
        <a href="/?page=<%= nextPage %>" class="pagination">&lt; View Older Posts</a>
      <% } %>
  
    
  
  
  
  </section>